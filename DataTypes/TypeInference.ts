//Type inference - If u don't specify any data type. at the runtime Ts will assign some datatype to this variable at runtime
                  //this process is called type inference. if u don't specify any data type TS will allocates the some data type to the variable based on the value which  we provided
                  //the ts will automatically assign the type to the variable
//Ex.

let x=30;  //define a variable